{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "cli": "tsx src/cli/main.ts",
    "validate": "tsx src/game/stories/mystery/story.validate.ts",
    "validate:scene": "tsx src/game/stories/mystery/scene.validate.ts",
    "test": "npm run validate && npm run validate:scene"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "gh-pages": "^6.3.0",
    "tsx": "^4.23.13",
    "typescript": "^5.0.0",
    "vite": "^8.3.0"
  }
}
